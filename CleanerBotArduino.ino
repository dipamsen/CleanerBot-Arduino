String mode = "manual";

// Code in setup() will run only once at start
void setup() {

}

// Code in loop() will run repeatedly
void loop() {
  if(mode == "auto") {
    // MOVEMENT AND OBSTACLE DETECTION
    // read input from ultrasonic sensor
    // if distance < 50 turn right
  }



  // BLUETOOTH AND REMOTE CONTROLLING
  // recieve messages from bluetooth
  // if message = "auto" then mode = "auto"
  // if message = "f" move forward
  // if message = "l" move left
  // if message = "r" move right
  // if message = "b" move backward

}
